import 'package:flutter/material.dart';

class Hades_Tap extends StatelessWidget {
  const Hades_Tap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}